# Client (Next.js)

- Node 20 LTS recommended
- Uses Next.js 15 and React 19
- Chatbot available at /chatbot, powered by OpenRouter model: `openai/gpt-oss-20b:free`
- Server-side API key: set `OPENROUTER_API_KEY` in environment (do not expose on client)
